# Minimal, robust dependency set. Tested intent: recent PyTorch + transformers.
# On RunPod, start from a "PyTorch 2.x / CUDA 12" image and just pip install this.
torch>=2.1
transformers>=4.40
safetensors>=0.4
numpy>=1.24
matplotlib>=3.7
